with outils, babysitter, famille, ada.Text_IO;
use outils, babysitter, famille, ada.Text_IO;

package tarifs is
   procedure tarif_garde (facture : out Integer; famille : T_famille; C : T_creneau);

end tarifs;
